# Water-QualityClassifier-
I developed a web-based Water Quality Classification system using Java Servlets and JDBC. The user inputs water parameters through a dashboard, and based on predefined thresholds, the system classifies water as safe or unsafe. The results are dynamically displayed and stored in a database for record tracking.

